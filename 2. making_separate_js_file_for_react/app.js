const heading = React.createElement(
  "h1",
  null,
  "Hello World from REACT (using separate app.js file)"
);

const reactRoot = ReactDOM.createRoot(document.getElementById("root"));

reactRoot.render(heading);
